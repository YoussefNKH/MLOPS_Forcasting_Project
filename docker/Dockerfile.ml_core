
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY src/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy source code
COPY src/ src/
# Set python path so imports like 'from src...' work
ENV PYTHONPATH=/app

# Default command
CMD ["python", "src/main.py"]
